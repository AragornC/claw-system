# 经验总结：从 Web 对话框到桌面应用的转向

> 本文档记录了 claw-system 第一阶段（Web 版本）的核心经验与教训，作为桌面应用阶段的起点参考。

---

## 一、第一阶段做了什么

### 整体方向
基于 Web 的量化交易辅助系统，核心思路是：**用户用自然语言描述想法，AI 自动生成可执行的交易特征/策略代码**。

### 主要模块
1. **策略意图工作台（Strategy Intent Workbench）**
   - 用户输入交易想法，AI 经过多个阶段自动生成特征代码
   - 生成流程：`understand` → `plan` → `spec_lock` → `write` → `run/detect/repair` → `summarize`
   - 每个阶段有独立的步骤卡片展示，支持流式渲染

2. **特征生成流水线**
   - AI 理解用户意图后，生成结构化 Plan（目标 / 方法 / 验证 / 修复策略）
   - 自动编写 Python 代码并在沙箱中运行验证
   - 多轮修复机制（最多 N 轮 detect → repair）
   - 生成后输出特征分布图（基于均值 / 标准差合成正态分布柱状图）

3. **特征验证与可视化**
   - 特征卡片（方案 C）：特征名 + 分类标签 + 历史分布柱状图 + 区间语义标注
   - 区间标注按 `mainCategory` 自动适配语义（波动类、趋势类、风控类等）
   - 二值型特征使用比例填充条替代柱状图

4. **前端渲染架构**
   - 纯原生 JS + HTML/CSS，无框架依赖
   - 所有组件写在单一 runtime 文件中，通过 closure 管理状态
   - SSE 流式接收后端事件，逐步渲染步骤进度

---

## 二、做得比较好的地方

### UI 设计流程
- **先做 Demo 再写代码** 的工作方式非常有效：通过一系列独立的 HTML demo 文件让用户确认视觉效果，再动代码，减少了大量反复修改
- 多方案并列对比（方案 A/B/C/D）比只给一个方案更容易让用户做出决策
- 修复前后对比 Demo 能直观展示改动效果，用户信任度更高

### 特征分布可视化
- 通过均值 + 标准差合成正态分布柱状图的思路可行，不需要原始数据就能给用户直观感受
- 分类型（bipolar / 普通 / 二值）的自适应渲染策略经过验证是正确方向
- 区间语义标注（「低值区：市场走势平稳」/ 「高值区：市场波动较大」）比显示技术统计数字对普通用户友好得多

### AI 生成流水线
- 多阶段分步骤的流水线设计思路清晰，每个阶段职责明确
- 多轮修复机制（detect → repair → re-run）实际效果稳定

---

## 三、Web 对话框的核心局限

### 1. 空间太小，信息密度受限
对话框的纵向空间决定了每次只能展示少量信息，专业交易需要同时看多个特征、多条 K 线、多策略参数对比，对话框装不下。

### 2. 无法做多窗口 / 多视图联动
专业工作流需要「左边看图、右边调参、底部看日志」的多面板布局，对话框天然是单列线性的，做不了。

### 3. 状态管理复杂度高
Web 版在单次 session 内可以，但跨 session 持久化、历史回放、策略版本管理在浏览器里做成本极高。

### 4. 性能瓶颈
大量 K 线数据、实时行情渲染在浏览器端有 JS 单线程瓶颈，桌面应用可以用多线程和原生渲染。

### 5. 部署和更新体验差
每次改 UI 都需要刷新页面、清缓存，对需要稳定运行的交易工具来说体验很差。

---

## 四、转向桌面应用的判断依据

- 交易越专业，需要的**信息密度越高**、**操作效率要求越高**，这是 Web 对话框的结构性缺陷，修不好
- 专业交易软件（如 TradingView 桌面版、Sierra Chart、NinjaTrader）都是桌面应用，不是 Web 对话框，有其道理
- 桌面应用可以原生调用本地文件系统、数据库、计算资源，适合量化策略的调试和回测场景
- AI 辅助功能可以作为桌面应用内的**一个面板/侧边栏**存在，而不是整个交互的容器

---

## 五、关键技术经验（可复用）

### 特征生成流水线设计
```
用户意图
  → understand（结构化需求）
  → plan（生成方案：目标 / 方法 / 输入 / 验证策略）
  → spec_lock（锁定技术规格）
  → write（生成代码）
  → run → detect → repair（验证与修复循环）
  → summarize（摘要 + 结果展示）
```
这个分层设计可以直接复用到桌面端，只需要换掉渲染层。

### 数据结构约定
- `feature.name` / `feature.mainCategory` — 特征元信息
- `runArtifacts.evaluationOutput.stats` — 运行统计（`mean` / `std` / `barCount`）
- `resultSummary` — 一句话总结，用于用户侧展示
- `planArtifact` / `specArtifact` / `generatedCode` — 各阶段产物

### 用户体验原则
- **技术数值对普通用户没意义**，要翻译成语义标注（「高波动市场」比「std=2.3」有用）
- **分布图比数字直观**，哪怕是合成的，也比表格有说服力
- **生成过程可见** 比 loading 圈圈好得多，用户知道 AI 在做什么才有信任感
- **特征只描述市场状态**，不做策略判断（「高值区风险敞口偏高」，不写「建议减仓」）

---

## 六、桌面应用阶段的起点建议

1. **技术栈选型**：考虑 Electron + React/Vue（复用 Web 技术栈），或 Tauri（更轻量，Rust 后端），或原生方案（Swift/macOS、Qt）
2. **核心布局**：多面板（行情图 / 策略调试 / AI 助手 / 日志）是专业交易软件的标配
3. **AI 集成方式**：AI 以「侧边面板」或「浮动助手」形式存在，而不是主界面
4. **本地优先**：策略代码、回测数据、特征库本地存储，云端只做同步和模型调用
5. **复用后端**：现有的特征生成流水线后端逻辑可以直接复用，只需要换 API 接入方式

---

*记录于 2026 年 3 月，第一阶段结束节点。*
